// Dynamic Memory Allocation(c++ is created in 1983)

#include <iostream>

using namespace std;
int main()
{
    // int *ptr_i = new int(25);
    // float *ptr_f = new float(10.1234);
    // char *ptr_c = new char('a');
    // double *ptr_d = new double(123.5564); 

    // cout << "\n\n\nContent of the Pointers " << endl;
    // cout << "Integer =" << *ptr_i << endl;
    // cout << "Float =" << *ptr_f << endl;
    // cout << "Character =" << *ptr_c << endl;
    // cout << "Double =" << *ptr_d << endl;

    // delete ptr_i;
    // delete ptr_f;
    // delete ptr_c;
    // delete ptr_d;

    // Array me runtime pe resize hoti hai

    int *ptr_a = new int[20];
    int *ptr_n = new int;

    cout << "\nHow many number are there";
}
